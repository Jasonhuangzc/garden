# 🌻 单词花园 (Vocabulary Garden)

一个激励背单词的互动花园应用，通过背单词赚取积分，用积分兑换金币种花。

## ✨ 功能特性

- **📚 背单词赚积分** - 每背10个单词可收取积分
- **🪙 积分兑换金币** - 积分自动转化为金币
- **🌸 购买种植花朵** - 5pts/朵，四种花可选
- **🏡 共享花园** - 双人协作，一起装扮花园
- **🔄 每日重置** - 北京时间凌晨4:30自动重置

## 🚀 快速开始

### 1. 配置 Firebase

在 [Firebase Console](https://console.firebase.google.com) 创建项目，启用 Firestore。

修改 `firebase-config.js` 填入你的配置：

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    // ...
};
```

### 2. 本地运行

```bash
python serve.py
```

访问 http://localhost:8000

### 3. 部署到 Firebase

```bash
firebase login
firebase deploy
```

## 📁 项目结构

```
garden/
├── index.html          # 主页面
├── admin.html          # 管理后台（导入数据、重置）
├── script.js           # 前端逻辑
├── game-logic.js       # 游戏核心逻辑（Firestore操作）
├── style.css           # 样式
├── firebase-config.js  # Firebase配置
└── image/              # 花朵图片资源
```

## 🔧 管理后台

访问 `/admin.html` 可以：
- 📥 导入抓包数据（粘贴JSON更新单词数据）
- ⚠️ 重置每日数据（测试用）

---

**Made with 💖**
